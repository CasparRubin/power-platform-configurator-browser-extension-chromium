/** Layout shell for the Flow Inspector side panel (full width/height of Chrome's panel). */
export const INSPECTOR_SHELL_CLASS =
  "flex h-full w-full min-h-0 min-w-0 flex-col overflow-hidden bg-background text-foreground";
